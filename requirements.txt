# ── Driving-CivicSense-Vision-Model ────────────────────
# Core dependencies. Add versions as modules stabilize.
#
# TODO: Pin exact versions once the pipeline is validated.

# Detection Backbone
ultralytics>=8.0.0

# Tracking
# boxmot>=1.0.0        # BoxMOT tracker (Deep SORT, BoT-SORT, etc.)

# Inference
onnxruntime>=1.15.0
onnx-simplifier>=0.4.0

# Image Processing
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Utilities
torch>=2.0.0           # For training (not needed at inference if using ONNX)
pyyaml>=6.0
tqdm>=4.65.0

# Visualization (dev/debug)
matplotlib>=3.7.0

# Testing
pytest>=7.4.0
pytest-benchmark>=4.0.0
